apiVersion: v1
kind: Service
metadata:
  name: appointment-scheduler-service
spec:
  type: LoadBalancer
  selector:
    app: appointment-scheduler
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
